models:
  model:
    provider: anthropic
    model: claude-sonnet-4-0
    max_tokens: 64000

commands:
  ci:
    deploy: "Deploy the app"

agents:
  dev:
    model: model
    use_commands: [nonexistent]
